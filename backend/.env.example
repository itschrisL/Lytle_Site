# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=["http://localhost:3000"]

# SMTP settings — leave blank to log contact messages instead of sending email
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
CONTACT_RECIPIENT=chris@example.com
